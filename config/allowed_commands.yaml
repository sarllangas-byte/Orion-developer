commands:
  allowed:
    - [python, -m, pytest, -q]
    - [python, -m, ruff, check, .]
    - [python, -m, mypy, .]
    - [npm, test]
    - [npm, run, lint]
    - [npm, run, build]

# Ces commandes sont exécutées sans shell. Le modèle ne peut pas en ajouter.
